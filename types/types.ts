export interface SmallCardItem {
  distance: string;
  img: string;
  location: string;
}

export interface MediumCardItem {
  img: string;
  title: string;
}

export interface LargeCardItem {
  img: string;
  title: string;
  description: string;
  buttonText: string;
}
